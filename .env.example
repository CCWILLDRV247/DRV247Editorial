# Single password for /admin (required in production).
# Local fallback is desk247 if this is unset.
ADMIN_PASSWORD=desk247

# Optional. Without these, YouTube and NewsAPI sources ingest mock items.
YOUTUBE_API_KEY=
NEWSAPI_KEY=

# Scheduled pull interval (default 15 minutes).
INGEST_INTERVAL_MS=900000
